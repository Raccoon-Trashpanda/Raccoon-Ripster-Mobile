# Ripster Android — трекинг задач

Живой список. `[x]` — сделано и проверено на эмуляторе, `[~]` — в работе,
`[ ]` — не начато. Правки владельца помечены **(владелец DD.MM)**.

## Сделано в этой серии сессий

- [x] Reference-плеер — вид по умолчанию (был «студийный»)
- [x] Спектр: оси/сетка/подписи/знак RIPSTER как в ПК, линейная шкала частоты
- [x] Спектр: честный статус «не удалось разобрать файл» вместо вечного «Строю…»
- [x] Плеер: транспорт `повтор·назад·PLAY·вперёд·перемешать`, назад/вперёд из ПК крупнее, анимация нажатия
- [x] Плеер: ряд квадратных кнопок под транспортом (Трек-лист/Текст/Спектр/Эквалайзер), закреплён снизу, всегда виден
- [x] Плеер: мёртвые кнопки ожили — shuffle/repeat дёргают Media3, ♥ переключается, меню-«⋯» убрано
- [x] Плеер: «as promised» → компактный знак `● расхождение / качество ок`
- [x] Плеер: эквалайзер прямо в плеере (нативный audiofx: пресеты + бас/стерео/громкость)
- [x] Плеер: `SeekStrip` вместо 3px-полоски — толщина 6/14, ручка при касании, ведение пальцем, строка времени с дельтой
- [x] Плеер: seek+транспорт+действия вынесены ИЗ verticalScroll → «скрол по треку» заработал
- [x] Плеер: амбиентная заливка фона по среднему цвету обложки (`rememberCoverAvg` + `clampCoverTint`)
- [x] Радар: заведён (316 отслеживаемых, лента релизов + «Скачать», переключатель Релизы/Подписки)
- [x] Поиск: сервисы свёрнуты в строку → меню с галочками, поиск сразу по всем отмеченным, результаты сливаются
- [x] Поиск: фильтры тип (Всё/Альбомы/Синглы/Треки) · сортировка · год
- [x] Поиск: таймаут 6с на `isConfigured()` — не висит в «Проверяю сервисы…»
- [x] Пояснения спрятаны под «?» (радар, спектр) — не занимают пол-экрана
- [x] Яндекс/учётки: авто-синк с ПК при каждом старте, если спарено
- [x] Скин Neon — тема по умолчанию, радиальные подсветки под всем UI, glow на play/«Скачать»/табах

## Правки владельца — сделано (092350–094700)

- [x] Плеер: убрана верхняя строка (шеврон + «ПЛЕЕР»)
- [x] Плеер: обложка максимальная (0.46·высоты, до 400dp), подписи в зоне прокрутки
- [x] Плеер: ♥ крупнее (44dp), выверенный контур (не обрезается), по центру блока текста
- [x] Плеер: подтверждение качества — ВИЗУАЛЬНОЕ (значок ✓/!/⚠ на углу обложки), не фразой; «M4A · 24-bit» осталось как факт
- [x] Плеер: транспорт точ-в-точ ПК neon (`design/v001/player.html .controls`): 🔀 ⏮ [play 64 круг+glow] ⏭ 🔁, все второстепенные 24dp, равный зазор
- [x] Плеер: тап по значку качества → «Всё о потоке» (инспектор: контейнер/кодек/MIME/частота/каналы/разрядность/битрейт/размер + полный спектр с осями + вердикт + срез + кирпичная стена + путь)
- [x] Нижнее меню: 5-я вкладка «Инструменты» (ключ-иконка)
- [x] Инструменты → Спектр: выбор ЛЮБОГО файла → спектрограмма на устройстве + вердикт
- [x] Инструменты → Конвертер: WAV (PCM16) и M4A/AAC 256k через `MediaCodec`/`MediaMuxer`, SAF-выход
- [x] Радар: карточки релизов как в ПК — квадрат, обложка (iTunes lookup по id альбома из ссылки), бейдж сервиса слева-вверху, NEW снизу, круглая play=скачать; сетка 2 колонки, скролл вниз
- [x] Радар: кнопка «⚙» в шапке → настройки радара + полный список артистов
- [x] Настройки → Радар: была заглушка «Soon» → реальный раздел (пояснение про ПК + список артистов)
- [x] Загрузки: кнопки «Очистить готовые» / «Очистить всё»
- [x] Анимации: переход между вкладками (плеер выезжает снизу / сворачивается вниз, соседи — кросс-слайд), мини-плеер въезжает снизу
- [x] Эквалайзер: потолки bass/stereo/loudness занижены (700) — против клиппинга («странный звук»)
- [x] Эмулятор: разрешение поднято до 1440×3120 @ 500dpi (Xiaomi-флагман); влезает 2 карточки в ряд + скролл
- [x] Спектр: детектор среза переписан на «относительно шумового пола» (порт ПК) — тихий настоящий ВЧ больше не метится как lossy

## Правки владельца — сделано (095000+)

- [x] Радар: поиск по артисту (поле сверху)
- [x] Радар: фильтр по сервису (чипы Все / apple / soundcloud / …)
- [x] Радар: индикатор прокрутки справа у сетки (у LazyGrid своего нет — нарисован)
- [x] Радар: **Spotify заведён** — `/api/pair/radar` подмешивает `spotify.py._build_feed` (+фолбэк на весь `_sp_artist_state`), схлопнуто до артиста; было 316 → 4205, из них 3885 spotify. Обложки Spotify — через ban-safe embed (i.scdn.co)
- [x] Радар: NEW-бейдж зелёный (#38E0A0), как в ПК-референсе, не розовый
- [x] Радар: `relTime` понимает дату-только (yyyy-MM-dd от Spotify) и будущие даты (предзаказ)
- [x] Настройки → Качество: блок «по сервисам» — свой тир для каждого сервиса из его `qualities()`
- [x] Ripster учитывает per-service тир при скачивании (`DownloadWorker` → `AppSettings.qualityPrefFor(serviceId, onWifi)`)
- [x] Плеер: «Всё о потоке» открывается тапом по строке качества (M4A · 16-bit… ⓘ), не по значку на обложке
- [x] Плеер: обложка — фолбэк на встроенную в файл картинку (`MediaMetadataRetriever.embeddedPicture`), если `artworkUrl` пуст
- [x] Настройки → Приложение: тумблер «Адаптивные цвета — под обложку» + строка «Акцентный цвет #FF4D8F»
- [x] Адаптивная заливка — ГЛОБАЛЬНАЯ, на весь экран Ripster (края в края) из AppShell, а не только в плеере; **плавный кросс-фейд** при смене трека (`animateColorAsState`/`animateFloatAsState`); ослаблена (была «пятном»)
- [x] Полоса рядом с кружком загрузки не пустует: краткий статус очереди (качается X · N%, N в очереди, N с ошибкой, N готово) → тап открывает «Загрузки»

## Правки владельца — сделано (100000+)

- [x] Шкала перемотки шире (8dp покой / 18dp касание)
- [x] «Полоска кэша» на шкале — buffered-сегмент (ExoPlayer.bufferedPosition → State.bufferedMs)
- [x] Ручка перемотки: тонкая (3dp), выступает ~2dp сверху/снизу в ПОКОЕ, растёт до 10dp/+4dp при взятии пальцем и плавно возвращается
- [x] HW/SW ускорение: manifest `hardwareAccelerated=true` + `largeHeap=true`; ProfileInstaller (baseline profile) активен
- [x] Перф: PlayerController на тике 500мс обновляет ТОЛЬКО позицию/буфер (`pushState(positionOnly=true)`), не пересобирает список очереди — минус аллокации и перекомпоновки
- [x] Иконка приложения — из аватара ТГ-бота (`tgbot/bot_avatar.png`): adaptive-icon v26 + legacy mipmap mdpi…xxxhdpi + round; метаданные вычищены при пересейве
- [x] Слово «Ripster» в шапке заменено на круглый аватар (`drawable/ic_ripster`)
- [x] Настройки → «О программе»: иконка, версия (PackageManager), описание, ссылка на репозиторий, «Проверить обновления» (GitHub releases API; репо `raccoon-ripster/ripster` — уточнить при релизе)
- [x] Аудит движков: все 8 клиентов реальны (130–350 строк), заглушек нет. Единственный `Soon` — Digs (Раскопки) в настройках; `MainActivity` demo-витрина не используется. Живая проверка: поиск «bonobo» → SoundCloud вернул реальные результаты с обложками, ошибок в logcat нет

## Правки владельца — сделано (первый тест-билд)

- [x] `Ripster-test-v0.1.apk` в корне — подписанный release (keystore `ripster-release.jks`), 12.6 МБ, md5 `7b6a6e58…` (норм: Android даёт медиа-стек сам, нет ffmpeg/python/docker; все 8 движков + конвертер + спектр в dex — проверено)
- [x] **Standalone-цепочка проверена вживую с ВЫКЛЮЧЕННОЙ app.py** (debug-билд, БД-доказательства): поиск → очередь → `DownloadWorker` → SoundCloud-движок → MP3 на диск → ID3v2 214 КБ (TIT2/TPE1/TALB/APIC-обложка) → `AudioProbe` → строка library (mp3/128/44100/138с) → воспроизведение через `c2.android.mp3.decoder`, `PlaybackState=PLAYING`. Release-билд: «2 готово» + системная нотификация загрузки, PC down.
- [x] **Фикс движка SoundCloud** (`SoundCloudClient.kt`): (1) перебор транскодингов с фолбэком — если `progressive` отдаёт 404 (SC 2024 массово ломает бесплатный progressive у лейбловых треков), идём на HLS mp3/aac; (2) `isNonDrm` больше НЕ пропускает CTR/CBC-зашифрованные адаптивные потоки (`abr_*` пресеты и `.../stream/ctr-encrypted-hls` в url; раньше `"mpeg" in mime` ложно принимал `audio/mpegurl`). DRM-only трек теперь падает с честным «только в FairPlay-зашифрованном HLS», а не голым HTTP 404.
- [x] APK отправлен владельцу в бот (`@Raccoon_Ripster_bot` → chat 521175329, message_id 17946)
- [x] Иконка приложения + аватар в шапке — реальная аватарка `@Raccoon_Ripster_bot` (стянута с Telegram API), метаданные вычищены
- [x] Сопряжение: **PC UI появился** — Настройки ПК → «📱 Сопряжение с телефоном» → «Показать код» (`/api/pair/start`, теперь 8 цифр) + отсчёт + тумблер «Делиться учётками» + «Отвязать все устройства» (`/api/pair/revoke-all`, loopback)
- [x] Безопасность сопряжения: лимит токенов 10→**3** (10 вытеснял токен реального телефона — отсюда «слетает после рестартов»); стало **обоюдное расцепление** — `PcBridge.unpair()` зовёт `/api/pair/unpair` (bearer) на ПК, потом чистит локально; `pairing_state.json` подрезан до 3, токен текущего телефона сохранён и проверен (radar HTTP 200)
- [x] Точки входа проверены: `/start` и `/revoke-all` — только loopback; `/claim` — одноразовый 8-значный код, TTL 5 мин, тормоз перебора 10/10мин; остальное — opaque bearer 256 бит; весь `/api/pair/` вне owner-cookie и CSRF (несёт свою аутентификацию)
- [x] «Всё о потоке» — по тапу на строку качества (не по значку на обложке); значок ✓ с обложки убран (остался цветной пункт-предупреждение в строке для mismatch/fake)
- [x] Ambilight переделан: НЕ картинка обложки, а мягкая **меш-заливка из средней палитры** (5 приглушённых зон, разложены по всему экрану, хаотично «дышат» по seed из альбом+трек; при смене альбома палитра перетекает, внутри альбома — та же палитра, пятна чуть светлее/темнее и переставляются). Плеер сделан прозрачным — ambilight теперь на всю площадь плеера, не только сверху
- [x] Перф: поллинг позиции 500→1000мс, на тике только позиция/буфер (без пересборки очереди); manifest `hardwareAccelerated=true` + `largeHeap=true`
- [x] Кружок загрузки в поиске больше не мерцает — убран второй экземпляр из оверлея поиска (заменён текстовым `DownloadStrip`)
- [x] Шкала перемотки: шире (8/18dp), «полоска кэша» (buffered), тонкая ручка выступает ~2dp и растёт при касании
- [x] «Что умеет мобильный Ripster» — 6 пунктов в Настройки → О программе, все через `tr()` (5 языков, показ по выбранному)
- [x] Радар: Spotify заведён (`/api/pair/radar` мешает `spotify._build_feed`), поиск+фильтр по сервису+бегунок, орб-карточки с реальными обложками (Apple iTunes lookup, Spotify embed), NEW-бейдж зелёный

## Правки владельца — сделано (03.09, по видео с ошибками + фиксы)

- [x] Downloads: обрезка «Retry»/«Cancel» за правым краем на узком экране (370dp) — действия вынесены на отдельную строку под заголовком, выравнивание вправо, у кнопок появились нормальные тап-зоны (`DownloadsQueueScreen.kt`)
- [x] Поиск: «Nothing found» над непустым списком результатов — баннер ошибки гасит «пусто»-сообщения (`search.nothing`/`search.filter_empty`), когда выдача на экране; ошибки уровня сервиса остаются (`SearchScreen.kt`)
- [x] Очередь: дубль трека («Jacob and the Stone» ×2) — гонка check-then-insert в `enqueue`; обёрнуто в `Mutex` (`DownloadQueue.kt`)
- [x] Home → FAILED DOWNLOADS: пустые серые плитки вместо обложек — `art` шёл жёстко `null`; теперь обложка парсится из `trackJson.artworkUrl` (`HomeScreen.kt`)
- [x] Поиск: ▶ по карточке трека/релиза — «молчаливый ноль» при многократном тапе. `playPending`-гейт (один резолв за раз, строка и кружок ▶ больше не сдваивают), мгновенный отклик «Включаю…», `try/catch` вокруг резолва (падение корутины больше не проглатывается), честное сообщение для Apple/SoundCloud (не стримятся на мобиле), `streamableAlbumUrl` расширен на yandex/beatport (`SearchScreen.kt`, `Strings.kt` +`search.starting`)
- Компиляция: `:app:compileDebugKotlin` — BUILD SUCCESSFUL. Живьём на эмуляторе НЕ проверено (эмулятор не поднят).

## Не начато / следующий заход (обсудить с владельцем)

- [ ] **Spotify нативный OGG-движок** (вход по логину/паролю Spotify, тянуть OGG из учётки) — это librespot-класс (сессия, AP-connect, audio-key, Vorbis-decrypt), НДК/Rust или полная реализация протокола = дни работы, НЕ в первый билд. Сейчас Spotify = конверсия по ISRC → Deezer/Qobuz
- [ ] Hi-res пикер для Spotify-ссылок — всплывающий выбор «скачать через Qobuz/Tidal/Deezer» + «запомнить выбор» (конверт-путь уже есть, нужен диалог + персист выбора)
- [ ] Рекурсивный сканер выбранной SAF-папки — находить аудио на всех уровнях вложенности, подхватывать в библиотеку (мульти-диск, mixed-версии). Сейчас библиотека = только то, что скачал сам
- [ ] Обложка плеера: фолбэк на встроенную в файл, если `artworkUrl` пуст
- [ ] Конвертер: FLAC (API 29+), MP3/OPUS через ПК
- [ ] Управление загрузками: пауза/резюм/приоритет (нужна переработка `DownloadQueue`)
- [ ] Раскопки (Digs) — заглушка «Soon»
- [ ] Внешние устройства вывода (MediaRouter) · Яндекс.Станция вживую
- [ ] github_setup: `ripster/routes/pairing.py` не зеркалится (нет `ripster/` в публичке)

## Не начато / следующий заход

- [ ] Обложка плеера: фолбэк на встроенную в файл картинку, если `artworkUrl` пустой (напр. «After Hours» — в БД пустой url)
- [ ] «Странный звук при проигрывании» — вероятно артефакты аудио самого эмулятора; проверить на железе + с выключенным EQ
- [ ] Управление загрузками: пауза/резюм (WorkManager без нативной паузы → cancel+retry), приоритет (нужна сериализованная очередь) — требует переработки `DownloadQueue`
- [ ] Конвертер: FLAC (API 29+), MP3/OPUS «через ПК» (`/api/pair/transcode`)
- [ ] Канвас-скин: круглая обложка с glow-кольцом (полная переработка раскладки плеера)
- [ ] Кнопка подписок в глобальном топ-баре (сейчас «⚙» в шапке радара)
- [ ] Импорт подписок из сервисов, где вошёл пользователь
- [ ] Полный «поиск по параметрам» уровня ПК `discovery.py` (продвинутый синтаксис Deezer, лейбл-фильтры)
- [ ] Внешние устройства вывода (MediaRouter / AudioDeviceInfo)
- [ ] Яндекс.Станция вживую · `aac_hq` SoundCloud · 3 режима fan-out по /ws
- [ ] Релиз-APK (`assembleRelease`, keystore `ripster-release.jks`)
- [ ] Зеркало github_setup — `ripster/routes/pairing.py`, точечные правки `app.py`/`auth.py`
